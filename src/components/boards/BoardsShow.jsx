import React from "react";
import { connect } from "react-redux";

const BoardsShow = () => {
  return (
    <React.Fragment>
      <h2>BoardsShow</h2>
    </React.Fragment>
  );
};

export default connect()(BoardsShow);
